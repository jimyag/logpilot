---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusterlogpilotpolicies.logpilot.logpilot.jimyag.com
spec:
  group: logpilot.logpilot.jimyag.com
  names:
    kind: ClusterLogPilotPolicy
    listKind: ClusterLogPilotPolicyList
    plural: clusterlogpilotpolicies
    singular: clusterlogpilotpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterLogPilotPolicy is the Schema for the clusterlogpilotpolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterLogPilotPolicySpec defines a cluster-scoped standalone
              pipeline (e.g. K8s events).
            properties:
              input:
                description: InputSpec defines a data source.
                properties:
                  batchLen:
                    default: 1000
                    type: integer
                  config:
                    additionalProperties:
                      x-kubernetes-preserve-unknown-fields: true
                    type: object
                  type:
                    description: 'Type is the input type: file, dir, k8sEvent, mongo.'
                    type: string
                required:
                - type
                type: object
              output:
                description: OutputSpec defines a data destination.
                properties:
                  batchInterval:
                    default: 300
                    type: integer
                  batchLen:
                    default: 500
                    type: integer
                  batchSize:
                    default: 5242880
                    type: integer
                  config:
                    additionalProperties:
                      x-kubernetes-preserve-unknown-fields: true
                    type: object
                  type:
                    description: 'Type is the output type: kafka, elasticsearch, http,
                      file.'
                    type: string
                required:
                - type
                type: object
              transforms:
                items:
                  description: TransformSpec defines a data transformation step.
                  properties:
                    batchLen:
                      default: 1000
                      type: integer
                    config:
                      additionalProperties:
                        x-kubernetes-preserve-unknown-fields: true
                      type: object
                    type:
                      description: 'Type is the transform type: json, label, drop,
                        regex, multiline.'
                      type: string
                  required:
                  - type
                  type: object
                type: array
            required:
            - input
            - output
            type: object
          status:
            description: ClusterLogPilotPolicyStatus defines the observed state of
              ClusterLogPilotPolicy.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

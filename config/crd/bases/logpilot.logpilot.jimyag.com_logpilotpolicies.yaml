---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: logpilotpolicies.logpilot.logpilot.jimyag.com
spec:
  group: logpilot.logpilot.jimyag.com
  names:
    kind: LogPilotPolicy
    listKind: LogPilotPolicyList
    plural: logpilotpolicies
    singular: logpilotpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LogPilotPolicy is the Schema for the logpilotpolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LogPilotPolicySpec defines the desired state of LogPilotPolicy.
              Policy names must be globally unique within the cluster.
              Either (Selector + Containers) or (Input + Output) must be set.
            properties:
              containers:
                items:
                  description: ContainerPolicy defines the log collection pipeline
                    for one container+logType pair.
                  properties:
                    batchInterval:
                      default: 300
                      type: integer
                    batchLen:
                      default: 1000
                      type: integer
                    batchSize:
                      default: 5242880
                      type: integer
                    clean:
                      description: |-
                        CleanSpec controls log file cleanup while a pod is running.
                        After pod deletion, all files are always cleaned once lag == 0.
                      properties:
                        interval:
                          default: 10
                          type: integer
                        reserveFileNumber:
                          default: 10
                          type: integer
                        reserveFileSize:
                          default: 10240
                          type: integer
                        retainDays:
                          type: integer
                        strategy:
                          default: afterCollected
                          enum:
                          - afterCollected
                          - retain
                          - never
                          type: string
                      type: object
                    collector:
                      default: host
                      enum:
                      - host
                      - sidecar
                      type: string
                    delivery:
                      default: guaranteed
                      enum:
                      - guaranteed
                      - bestEffort
                      type: string
                    input:
                      description: InputSpec defines a data source.
                      properties:
                        batchLen:
                          default: 1000
                          type: integer
                        config:
                          additionalProperties:
                            x-kubernetes-preserve-unknown-fields: true
                          type: object
                        type:
                          description: Type is the input type.
                          enum:
                          - file
                          - dir
                          - k8sEvent
                          type: string
                      required:
                      - type
                      type: object
                    logType:
                      type: string
                    name:
                      type: string
                    output:
                      description: OutputSpec defines a data destination.
                      properties:
                        batchInterval:
                          default: 300
                          type: integer
                        batchLen:
                          default: 500
                          type: integer
                        batchSize:
                          default: 5242880
                          type: integer
                        config:
                          additionalProperties:
                            x-kubernetes-preserve-unknown-fields: true
                          type: object
                        type:
                          description: Type is the output type.
                          enum:
                          - http
                          - file
                          type: string
                      required:
                      - type
                      type: object
                    path:
                      description: Path is the container-internal log directory. Use
                        "-" for stdout/stderr.
                      type: string
                    transforms:
                      items:
                        description: TransformSpec defines a data transformation step.
                        properties:
                          batchLen:
                            default: 1000
                            type: integer
                          config:
                            additionalProperties:
                              x-kubernetes-preserve-unknown-fields: true
                            type: object
                          type:
                            description: Type is the transform type.
                            enum:
                            - json
                            - label
                            - drop
                            type: string
                        required:
                        - type
                        type: object
                      type: array
                  required:
                  - logType
                  - name
                  - output
                  - path
                  type: object
                type: array
              input:
                description: InputSpec defines a data source.
                properties:
                  batchLen:
                    default: 1000
                    type: integer
                  config:
                    additionalProperties:
                      x-kubernetes-preserve-unknown-fields: true
                    type: object
                  type:
                    description: Type is the input type.
                    enum:
                    - file
                    - dir
                    - k8sEvent
                    type: string
                required:
                - type
                type: object
              output:
                description: OutputSpec defines a data destination.
                properties:
                  batchInterval:
                    default: 300
                    type: integer
                  batchLen:
                    default: 500
                    type: integer
                  batchSize:
                    default: 5242880
                    type: integer
                  config:
                    additionalProperties:
                      x-kubernetes-preserve-unknown-fields: true
                    type: object
                  type:
                    description: Type is the output type.
                    enum:
                    - http
                    - file
                    type: string
                required:
                - type
                type: object
              selector:
                description: |-
                  A label selector is a label query over a set of resources. The result of matchLabels and
                  matchExpressions are ANDed. An empty label selector matches all objects. A null
                  label selector matches no objects.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              transforms:
                items:
                  description: TransformSpec defines a data transformation step.
                  properties:
                    batchLen:
                      default: 1000
                      type: integer
                    config:
                      additionalProperties:
                        x-kubernetes-preserve-unknown-fields: true
                      type: object
                    type:
                      description: Type is the transform type.
                      enum:
                      - json
                      - label
                      - drop
                      type: string
                  required:
                  - type
                  type: object
                type: array
            type: object
          status:
            description: LogPilotPolicyStatus defines the observed state of LogPilotPolicy.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
